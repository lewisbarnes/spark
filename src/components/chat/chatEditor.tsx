import { FC, createRef,  KeyboardEvent, useEffect, useMemo, useState } from "react";
import { } from 'react';
import { BaseEditor, createEditor, Descendant, Node } from 'slate';
import { Editable, ReactEditor, Slate, withReact } from 'slate-react';

export const ChatEditor: FC = () => {
	return <div></div>;
}